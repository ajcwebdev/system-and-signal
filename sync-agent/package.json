{
  "name": "sync-agent",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "setup": "bun scripts/setup.ts",
    "up": "docker compose up -d --wait",
    "powersync:start": "docker compose start powersync",
    "powersync:stop": "docker compose stop powersync",
    "start": "node --env-file=.env index.ts",
    "dev": "node --env-file=.env --watch index.ts",
    "check": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@powersync/node": "^0.18.3",
    "better-sqlite3": "^12.8.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
